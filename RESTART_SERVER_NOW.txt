✅ ĐÃ FIX TẤT CẢ IMPORTS!

Các thay đổi:
- UI components cũ (button, dropdown, etc.) → src/components/ui/
- UI components mới (dialog, alert, etc.) → components/ui/
- Hooks → hooks/
- Lib → lib/

BÂY GIỜ:

1. Stop server (Ctrl+C)
2. Clear cache:
   rm -rf .next
3. Chạy lại:
   npm run dev

Sau đó mở browser và test!
