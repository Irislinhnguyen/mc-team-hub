# ==============================================
# Environment Variables Template
# ==============================================
# Copy this file to .env and fill in your values
# NEVER commit .env to git!

# ==============================================
# Google Cloud / BigQuery Configuration
# ==============================================
NEXT_PUBLIC_GOOGLE_CLOUD_PROJECT=your-gcp-project-id
NEXT_PUBLIC_BIGQUERY_DATASET=GI_publisher
NEXT_PUBLIC_BIGQUERY_AI_DATASET=geniee_ai

# Google Cloud Service Account Credentials (JSON format)
# Generate from: https://console.cloud.google.com/iam-admin/serviceaccounts
# Required permissions: BigQuery Data Viewer, BigQuery Job User
GOOGLE_APPLICATION_CREDENTIALS_JSON={"type":"service_account","project_id":"your-project","private_key_id":"...","private_key":"...","client_email":"..."}

# ==============================================
# Google OAuth 2.0 Configuration
# ==============================================
# Create OAuth credentials at: https://console.cloud.google.com/apis/credentials
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
NEXT_PUBLIC_GOOGLE_CLIENT_SECRET=your-client-secret

# Domain restriction for OAuth (only emails from this domain can login)
NEXT_PUBLIC_ALLOWED_DOMAIN=yourdomain.com

# Application URL (used for OAuth redirects)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ==============================================
# JWT Authentication
# ==============================================
# SECURITY: Generate a strong random string (minimum 32 characters)
# You can generate one using: openssl rand -base64 32
# Or: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET_KEY=CHANGE-THIS-TO-A-STRONG-RANDOM-STRING-MINIMUM-32-CHARS

# ==============================================
# Supabase Configuration
# ==============================================
# Get these from: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ==============================================
# OpenAI Configuration (Optional)
# ==============================================
# Only needed if using AI features
# Get API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4-turbo-preview

# ==============================================
# Application Configuration
# ==============================================
NODE_ENV=development
PORT=3000

# ==============================================
# Security Notes
# ==============================================
# 1. NEVER commit this file with real values to git
# 2. The .env file is already in .gitignore
# 3. Rotate all secrets if they are ever exposed
# 4. Use different secrets for dev/staging/production
# 5. JWT_SECRET_KEY must be at least 32 characters
# 6. Always use HTTPS in production
