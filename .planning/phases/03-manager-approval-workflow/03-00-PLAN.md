---
phase: 03-manager-approval-workflow
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/e2e/approvals.spec.ts
  - tests/e2e/fixtures/auth.setup-admin-approver.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "E2E test file exists with approval workflow test cases"
    - "Auth fixtures exist for Manager and Leader roles"
    - "Tests can run with Playwright E2E framework"
  artifacts:
    - path: tests/e2e/approvals.spec.ts
      provides: Playwright E2E tests for approval workflow
      min_lines: 100
    - path: tests/e2e/fixtures/auth.setup-admin-approver.ts
      provides: Auth fixtures for Manager/Leader role testing
      min_lines: 50
  key_links:
    - from: approvals.spec.ts
      to: auth.setup-admin-approver.ts
      via: Import statements
      pattern: "import.*setup.*approver"
---

<objective>
Generate test infrastructure for Manager approval workflow: Playwright E2E tests and auth fixtures for Manager/Leader roles.

Purpose: Create test scaffolding that later plans can use to verify the approval workflow. Tests will run after implementation is complete.
Output: E2E test file with approval workflow test cases and auth fixtures for role-based testing.
</objective>

<execution_context>
@/Users/nguyenthuylinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nguyenthuylinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-manager-approval-workflow/03-CONTEXT.md
@.planning/phases/03-manager-approval-workflow/03-RESEARCH.md

@tests/e2e/fixtures/auth.fixture.ts
@playwright.config.ts
</context>

<interfaces>
<!-- Key types and contracts from existing codebase -->

From existing auth fixtures (if they exist):
```typescript
// Use existing auth patterns from tests/e2e/fixtures/auth.fixture.ts
```

From 03-CONTEXT.md user roles:
```
- Leader: Can grade and submit for review
- Manager: Can review, edit grades, and approve
- Admin: Same permissions as Manager
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create auth fixtures for Manager and Leader roles</name>
  <files>tests/e2e/fixtures/auth.setup-admin-approver.ts</files>
  <read_first>
    tests/e2e/fixtures/auth.fixture.ts
  </read_first>
  <action>
    Create auth fixtures at tests/e2e/fixtures/auth.setup-admin-approver.ts:

    1. **Export fixture functions:**
       - export async function setupManagerTest(page: Page): Promise<TestUser>
       - export async function setupLeaderTest(page: Page): Promise<TestUser>
       - export async function setupAdminTest(page: Page): Promise<TestUser>
       - export interface TestUser { email: string; password: string; name: string; role: string; id: string }

    2. **Manager fixture:**
       - Create or use existing Manager user from test database
       - Log in as Manager using existing auth flow
       - Return TestUser object with Manager credentials

    3. **Leader fixture:**
       - Create or use existing Leader user from test database
       - Log in as Leader using existing auth flow
       - Return TestUser object with Leader credentials

    4. **Admin fixture (if not exists):**
       - Create or use existing Admin user from test database
       - Log in as Admin using existing auth flow
       - Return TestUser object with Admin credentials

    5. **Use existing auth patterns:**
       - Import and reuse existing login functions from tests/e2e/fixtures/auth.fixture.ts
       - Follow same patterns for user creation and authentication

    6. **Clean up function:**
       - export async function cleanupTestUsers(): Promise<void>
       - Delete test users created during testing
       - Call in afterAll() hook

    Follow existing test fixture patterns in the codebase.
  </action>
  <verify>
    <automated>grep -q "setupManagerTest" tests/e2e/fixtures/auth.setup-admin-approver.ts && grep -q "setupLeaderTest" tests/e2e/fixtures/auth.setup-admin-approver.ts && grep -q "TestUser" tests/e2e/fixtures/auth.setup-admin-approver.ts</automated>
  </verify>
  <done>
    Auth fixtures file exists with setupManagerTest, setupLeaderTest, and setupAdminTest functions; each function creates/logs in user with correct role; TestUser interface defined; cleanup function provided.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Generate E2E test file for approval workflow</name>
  <files>tests/e2e/approvals.spec.ts</files>
  <read_first>
    tests/e2e/fixtures/auth.setup-admin-approver.ts
  </read_first>
  <action>
    Generate E2E test file at tests/e2e/approvals.spec.ts with test cases for approval workflow:

    1. **Import dependencies:**
       - import { test, expect, Page } from '@playwright/test'
       - import { setupManagerTest, setupLeaderTest, TestUser } from './fixtures/auth.setup-admin-approver'

    2. **Test suite structure:**
       - test.describe('Manager Approval Workflow', () => { ... })
       - Define test variables for manager, leader, submission, challenge

    3. **Test 1: Leader submits graded submission for review**
       - test('Leader can submit graded submission for review', async ({ page }) => { ... })
       - Setup: Login as Leader, navigate to grading page
       - Action: Grade all essay questions, click "Submit for Review"
       - Expect: Status changes to pending_review, notification sent to Manager

    4. **Test 2: Manager views pending approvals**
       - test('Manager can view pending approvals queue', async ({ page }) => { ... })
       - Setup: Login as Manager, navigate to /admin/approvals
       - Expect: See table with pending submissions
       - Expect: Filters work (challenge, team, leader)

    5. **Test 3: Manager edits grades before approval**
       - test('Manager can edit grades in approval detail view', async ({ page }) => { ... })
       - Setup: Login as Manager, open approval detail for submission
       - Action: Change essay score, add feedback, click "Save Changes"
       - Expect: Grades updated, modification tracked to Manager

    6. **Test 4: Manager approves submission**
       - test('Manager can approve submission', async ({ page }) => { ... })
       - Setup: Login as Manager, open approval detail
       - Action: Click "Approve" button
       - Expect: Status changes to approved, Leader notified, removed from queue

    7. **Test 5: Manager/Admin publishes approved submission**
       - test('Manager can publish approved submission', async ({ page }) => { ... })
       - Setup: Login as Manager, navigate to approved submission
       - Action: Click "Publish" button
       - Expect: Status changes to published, leaderboard updated, all users notified

    8. **Test 6: Leader cannot publish (permission check)**
       - test('Leader cannot publish submission', async ({ page }) => { ... })
       - Setup: Login as Leader, navigate to approved submission
       - Action: Attempt to publish
       - Expect: 403 Forbidden or publish button disabled/hidden

    9. **Test 7: Audit trail verification**
       - test('Approval actions create audit records', async ({ page }) => { ... })
       - Setup: Run through approval workflow
       - Action: Query approvals table via API or admin
       - Expect: Records for submitted_for_review, approved, published actions with correct user_id and timestamps

    10. **Test 8: Next/Previous navigation in approval queue**
        - test('Manager can navigate between pending submissions', async ({ page }) => { ... })
        - Setup: Login as Manager, create 3 pending submissions
        - Action: Open first submission, click Next twice
        - Expect: Navigate through all submissions, Previous button works

    11. **Setup and teardown:**
        - beforeAll: Create test users (Manager, Leader, Admin), create test challenge and submission
        - afterAll: Cleanup test data

    12. **Use page object patterns:**
        - Create helper functions for common actions: loginAs(user), navigateToApprovals(), submitForReview(submissionId), approve(submissionId)
        - This keeps tests clean and maintainable

    Note: These tests will FAIL until the implementation is complete. That's expected — this is test scaffolding.
  </action>
  <verify>
    <automated>grep -q "test.describe" tests/e2e/approvals.spec.ts && grep -q "Leader can submit" tests/e2e/approvals.spec.ts && grep -q "Manager can approve" tests/e2e/approvals.spec.ts && wc -l tests/e2e/approvals.spec.ts | grep -q "[1-9][0-9][0-9]"</automated>
  </verify>
  <done>
    E2E test file exists with test suite for approval workflow; includes at least 8 test cases covering submit, review, edit, approve, publish, permissions, audit trail, and navigation; tests are structured with proper setup/teardown; tests use auth fixtures from Task 1.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Auth fixtures exist for Manager, Leader, and Admin roles
2. E2E test file has comprehensive test cases for approval workflow
3. Tests are structured and will compile (even if they fail until implementation)
4. Test file imports and uses auth fixtures correctly
5. Tests cover the critical user paths through the approval workflow
</verification>

<success_criteria>
1. tests/e2e/approvals.spec.ts exists with 8+ test cases
2. tests/e2e/fixtures/auth.setup-admin-approver.ts exists with role fixtures
3. Tests can be run with npx playwright test approvals.spec.ts
4. Tests are properly structured and follow Playwright conventions
</success_criteria>

<output>
After completion, create `.planning/phases/03-manager-approval-workflow/03-00-SUMMARY.md` with:
- Test infrastructure created
- E2E test cases overview
- Auth fixtures for role-based testing
- Instructions for running tests after implementation
</output>
