---
phase: 03-manager-approval-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260318_add_approval_workflow.sql
  - apps/web/lib/supabase/database.types.ts
autonomous: true
requirements:
  - APPR-01
  - APPR-02
  - APPR-13

must_haves:
  truths:
    - "Submission status can be pending_review or approved"
    - "All approval actions are recorded in approvals table"
    - "Database types include new statuses and approvals table"
  artifacts:
    - path: supabase/migrations/20260318_add_approval_workflow.sql
      provides: Database schema for approval workflow
      min_lines: 80
      contains: "pending_review", "approved", "approvals", "CREATE TABLE"
    - path: apps/web/lib/supabase/database.types.ts
      provides: TypeScript types for approval workflow
      contains: "pending_review", "approved", "approvals"
  key_links:
    - from: Migration file
      to: database.types.ts
      via: Type regeneration command
      pattern: "npx supabase gen types typescript"
---

<objective>
Create database schema for approval workflow: extend submission status enum with pending_review and approved states, create approvals audit trail table, and regenerate TypeScript types.

Purpose: Enable Leader → Manager approval workflow by adding status states and audit tracking to database schema.
Output: Database migration file with new statuses and approvals table, updated TypeScript types.
</objective>

<execution_context>
@/Users/nguyenthuylinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nguyenthuylinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-manager-approval-workflow/03-CONTEXT.md
@.planning/phases/03-manager-approval-workflow/03-RESEARCH.md

@supabase/migrations/20260120_create_challenges.sql
@apps/web/lib/supabase/database.types.ts
</context>

<interfaces>
<!-- Key types and contracts from existing codebase -->

From supabase/migrations/20260120_create_challenges.sql:
```sql
-- Existing status constraint
challenge_submissions_status_check:
  CHECK (status IN (
    'in_progress',
    'submitted',
    'grading',
    'published'
  ))
```

From packages/auth/server.ts:
```typescript
export function isAdminOrManager(user: TokenPayload): boolean
export function isLeaderOrAbove(user: TokenPayload): boolean
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create approval workflow database migration</name>
  <files>supabase/migrations/20260318_add_approval_workflow.sql</files>
  <read_first>
    supabase/migrations/20260120_create_challenges.sql
  </read_first>
  <action>
    Create migration file supabase/migrations/20260318_add_approval_workflow.sql with:

    1. **Extend challenge_submissions status CHECK constraint:**
       - Drop existing challenge_submissions_status_check constraint
       - Add new constraint with values: 'in_progress', 'submitted', 'grading', 'pending_review', 'approved', 'published'
       - Use exact SQL: ALTER TABLE public.challenge_submissions DROP CONSTRAINT IF EXISTS challenge_submissions_status_check;
       - Use exact SQL: ALTER TABLE public.challenge_submissions ADD CONSTRAINT challenge_submissions_status_check CHECK (status IN ('in_progress', 'submitted', 'grading', 'pending_review', 'approved', 'published'));

    2. **Create approvals table:**
       - id: UUID PRIMARY KEY DEFAULT gen_random_uuid()
       - submission_id: UUID NOT NULL REFERENCES public.challenge_submissions(id) ON DELETE CASCADE
       - user_id: UUID NOT NULL REFERENCES public.users(id)
       - user_role: TEXT NOT NULL CHECK (user_role IN ('leader', 'manager', 'admin'))
       - action: TEXT NOT NULL CHECK (action IN ('submitted_for_review', 'approved'))
       - from_status: TEXT NOT NULL
       - to_status: TEXT NOT NULL
       - notes: TEXT (nullable, for future use)
       - created_at: TIMESTAMPTZ DEFAULT NOW()

    3. **Create indexes:**
       - idx_approvals_submission on submission_id
       - idx_approvals_user on user_id
       - idx_approvals_action on action

    4. **Enable RLS on approvals table:**
       - ALTER TABLE public.approvals ENABLE ROW LEVEL SECURITY

    5. **Create RLS policies for approvals:**
       - "All users can read approvals": SELECT using (auth.uid() IS NOT NULL)
       - "Leader/Manager/Admin can create approvals": INSERT with CHECK (EXISTS (SELECT 1 FROM public.users WHERE id = auth.uid() AND role IN ('leader', 'manager', 'admin')))
       - "No delete policy" (audit trail - no deletes allowed)

    Follow existing migration patterns from 20260120_create_challenges.sql for structure and formatting.
  </action>
  <verify>
    <automated>grep -q "pending_review" supabase/migrations/20260318_add_approval_workflow.sql && grep -q "approved" supabase/migrations/20260318_add_approval_workflow.sql && grep -q "CREATE TABLE.*approvals" supabase/migrations/20260318_add_approval_workflow.sql && grep -q "idx_approvals_submission" supabase/migrations/20260318_add_approval_workflow.sql</automated>
  </verify>
  <done>
    Migration file created with extended status enum including pending_review and approved, approvals table with all required columns, indexes for performance, and RLS policies for role-based access.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Regenerate TypeScript database types</name>
  <files>apps/web/lib/supabase/database.types.ts</files>
  <read_first>
    apps/web/lib/supabase/database.types.ts
  </read_first>
  <action>
    Regenerate TypeScript types to include new approval schema:

    1. **Run Supabase type generation:**
       - Execute: npx supabase gen types typescript --project-id YOUR_PROJECT_ID --schema public > apps/web/lib/supabase/database.types.ts
       - OR if using local Supabase CLI: npx supabase db types --schema public > apps/web/lib/supabase/database.types.ts

    2. **Verify new types are generated:**
       - challenge_submissions.Row.status should include 'pending_review' and 'approved'
       - New 'approvals' table with Row, Insert, Update types
       - approvals columns: id, submission_id, user_id, user_role, action, from_status, to_status, notes, created_at

    Note: The exact command depends on how the project is configured. Use the same method that was used to generate the existing database.types.ts file.
  </action>
  <verify>
    <automated>grep -q "pending_review" apps/web/lib/supabase/database.types.ts && grep -q "'approved'" apps/web/lib/supabase/database.types.ts && grep -q "approvals:" apps/web/lib/supabase/database.types.ts</automated>
  </verify>
  <done>
    TypeScript types regenerated with new submission status values (pending_review, approved) and approvals table types (Row, Insert, Update) including all columns.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Database migration file for approval workflow schema</what-built>
  <how-to-verify>
    1. Review the migration file at supabase/migrations/20260318_add_approval_workflow.sql
    2. Verify it includes:
       - Extended status constraint with pending_review and approved
       - approvals table with correct columns and constraints
       - RLS policies for proper access control
    3. Run the migration on your Supabase instance:
       - Via Supabase Dashboard: SQL Editor → paste migration content → Run
       - OR via CLI: supabase db push (if configured)
    4. After running migration, verify in Supabase Dashboard:
       - Table inspector shows approvals table exists
       - challenge_submissions status has new values in CHECK constraint
       - RLS policies are enabled
    5. Type "migration applied" when complete
  </how-to-verify>
  <resume-signal>Type "migration applied" to confirm the database schema has been updated on Supabase</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. Migration file exists with proper SQL syntax
2. TypeScript types include new approval-related types
3. Migration has been run on Supabase (manual checkpoint)
4. Approvals table is queryable via Supabase client
5. challenge_submissions.status accepts pending_review and approved values
</verification>

<success_criteria>
1. Database migration file created with valid SQL
2. Migration successfully applied to Supabase
3. TypeScript types regenerated and include approval workflow types
4. Approvals table exists in database with proper constraints and RLS
5. challenge_submissions status enum includes pending_review and approved
</success_criteria>

<output>
After completion, create `.planning/phases/03-manager-approval-workflow/03-01-SUMMARY.md` with:
- Migration details (new statuses, approvals table structure)
- RLS policies created
- TypeScript types added
- Database verification results
</output>
