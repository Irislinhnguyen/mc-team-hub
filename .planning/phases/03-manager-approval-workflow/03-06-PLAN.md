---
phase: 03-manager-approval-workflow
plan: 06
type: execute
wave: 6
depends_on: [03-03]
files_modified:
  - apps/web/app/api/challenges/[id]/publish/route.ts
autonomous: true
requirements:
  - APPR-12
  - APPR-13
  - APPR-14

must_haves:
  truths:
    - "Publish API rejects non-approved submissions"
    - "Publish API requires Manager or Admin role"
    - "Publish triggers notification to all users"
    - "Audit trail exists for all approval actions"
  artifacts:
    - path: apps/web/app/api/challenges/[id]/publish/route.ts
      provides: POST endpoint for publishing approved submissions
      exports: ["POST"]
  key_links:
    - from: publish API
      to: workflowNotificationService
      via: notifyUsersScoresPublished call
      pattern: "notifyUsersScoresPublished\\("
    - from: publish API
      to: challenge_submissions table
      via: Status check before publish
      pattern: "status.*eq.*approved"
---

<objective>
Modify publish API to enforce Manager approval before publishing: validate submission status is 'approved', require Manager/Admin role, and trigger user notifications.

Purpose: Ensure only Manager-approved submissions can be published to leaderboard, with proper role enforcement and user notifications.
Output: Updated publish API with approval validation and notification integration.
</objective>

<execution_context>
@/Users/nguyenthuylinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nguyenthuylinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-manager-approval-workflow/03-CONTEXT.md
@.planning/phases/03-manager-approval-workflow/03-RESEARCH.md
@.planning/phases/03-manager-approval-workflow/03-03-SUMMARY.md

@apps/web/app/api/challenges/[id]/publish/route.ts
@apps/web/app/api/challenges/[id]/grading/route.ts
@apps/web/lib/services/workflowNotificationService.ts
@packages/auth/server.ts
</context>

<interfaces>
<!-- Key types and contracts from existing codebase -->

From packages/auth/server.ts:
```typescript
export async function getServerUser(): Promise<TokenPayload | null>
export function isAdminOrManager(user: TokenPayload): boolean
```

From apps/web/lib/services/workflowNotificationService.ts:
```typescript
export async function notifyUsersScoresPublished(
  challengeId: string,
  challengeName: string
): Promise<void>
```

From database.types.ts (after Plan 03-01):
```typescript
type SubmissionStatus = 'in_progress' | 'submitted' | 'grading' | 'pending_review' | 'approved' | 'published'
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add approval validation to publish API</name>
  <files>apps/web/app/api/challenges/[id]/publish/route.ts</files>
  <read_first>
    apps/web/app/api/challenges/[id]/publish/route.ts
    packages/auth/server.ts
    apps/web/lib/services/workflowNotificationService.ts
    apps/web/app/api/challenges/[id]/grading/route.ts
  </read_first>
  <behavior>
    - Test 1: POST /api/challenges/{id}/publish with Manager auth and approved submission returns 200 and publishes
    - Test 2: POST with non-Manager auth returns 403 Forbidden
    - Test 3: POST with submission not in 'approved' status returns 400 "Grades must be approved before publishing"
    - Test 4: Successful publish triggers notification to all users
    - Test 5: Successful publish updates submission status to 'published'
  </behavior>
  <action>
    Modify the publish API endpoint at apps/web/app/api/challenges/[id]/publish/route.ts:

    1. **Read existing publish endpoint:**
       - Locate the existing POST handler in the publish route
       - Understand current logic (likely sets status to 'published' and updates leaderboard_published_at)
       - Note: If publish endpoint doesn't exist, create it following existing API patterns

    2. **Add Manager/Admin role check:**
       - After authentication: const user = await getServerUser()
       - Add role validation: if (!isAdminOrManager(user)) return NextResponse.json({ error: 'Only Manager or Admin can publish' }, { status: 403 })
       - This prevents Leaders from publishing (per CONTEXT.md locked decision)

    3. **Add approval status validation:**
       - Before publishing: query submission to check current status
       - const { data: submission } = await supabase.from('challenge_submissions').select('id, status, challenge_id').eq('id', submissionId).single()
       - if (!submission) return 404 Not Found
       - if (submission.status !== 'approved') return NextResponse.json({ error: 'Grades must be approved before publishing' }, { status: 400 })
       - This enforces APPR-12: Leaderboard publishing blocked until Manager approval

    4. **Update submission to published:**
       - update challenge_submissions set status = 'published', leaderboard_published_at = NOW() where id = submissionId and status = 'approved'
       - if no rows updated, return 400 (status changed concurrently)

    5. **Query challenge for notification:**
       - select id, name from challenges where id = submission.challenge_id

    6. **Trigger notification to all users:**
       - import { notifyUsersScoresPublished } from '@/lib/services/workflowNotificationService'
       - await notifyUsersScoresPublished(challenge.id, challenge.name)
       - Wrap in try-catch, log errors with [Publish API] prefix but don't fail request
       - This satisfies APPR-14: Notification sent to users when scores published

    7. **Log audit trail:**
       - Note: The approvals table already tracks the approve action (from pending_review to approved)
       - No additional approval record needed for publish (status change from approved to published is the record)
       - This satisfies APPR-13: Audit trail for all approval actions (already created by approve endpoint)

    8. **Error handling:**
       - Use existing error handling patterns from grading/route.ts
       - Log all errors with [Publish API] prefix
       - Return proper HTTP status codes (400 for bad state, 403 for forbidden, 404 for not found, 500 for server errors)

    9. **Return response:**
       - { success: true, submissionId, status: 'published', publishedAt: new Date().toISOString() }

    Follow existing API route patterns. Use consistent logging and error handling.
  </action>
  <verify>
    <automated>grep -q "isAdminOrManager" apps/web/app/api/challenges/\[id\]/publish/route.ts && grep -q "approved" apps/web/app/api/challenges/\[id\]/publish/route.ts && grep -q "notifyUsersScoresPublished" apps/web/app/api/challenges/\[id\]/publish/route.ts</automated>
  </verify>
  <done>
    Publish API validates submission status is 'approved' before publishing, requires Manager/Admin role, updates status to 'published', triggers notification to all users, and maintains audit trail through existing approvals table.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Publish API with approval validation and user notifications</what-built>
  <how-to-verify>
    1. Test API directly or use the admin interface:
       - Create or use a submission with status='grading'
       - As a Leader, grade the submission and submit for review (should become pending_review)
       - As a Manager, approve the submission (should become approved)
       - As a Manager, publish the submission
    2. Verify each step:
       - Leader cannot publish (403 Forbidden)
       - Publish fails when status is not approved (400 error)
       - Publish succeeds when status is approved
       - Status changes to 'published' after successful publish
       - All users receive notification "Scores Published" after publish
    3. Check audit trail in database:
       - approvals table should have records for submitted_for_review and approved actions
    4. Type "published" if the complete workflow works, or describe issues
  </how-to-verify>
  <resume-signal>Type "published" if approval workflow end-to-end works correctly, or describe issues</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. Publish API requires Manager/Admin role
2. Publish API validates submission status is 'approved'
3. Publish API returns 400 error when trying to publish non-approved submission
4. Publish API updates submission status to 'published'
5. Publish API triggers notification to all users
6. Audit trail exists for all approval actions (approvals table)
7. Complete workflow works: grade → submit → approve → publish
</verification>

<success_criteria>
1. Leaderboard publishing is blocked until Manager approval
2. Only Manager/Admin can publish to leaderboard
3. Users receive notification when scores are published
4. Complete audit trail exists for all approval actions
5. End-to-end workflow works: Leader grades → Manager approves → Manager/Admin publishes
</success_criteria>

<output>
After completion, create `.planning/phases/03-manager-approval-workflow/03-06-SUMMARY.md` with:
- Publish API validation logic
- Role enforcement implementation
- Notification integration details
- Audit trail verification
- End-to-end workflow validation results
</output>
