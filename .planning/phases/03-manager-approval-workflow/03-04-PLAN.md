---
phase: 03-manager-approval-workflow
plan: 04
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - apps/web/app/components/admin/approvals/SubmitForReviewButton.tsx
  - apps/web/app/components/admin/approvals/index.ts
  - apps/web/app/(protected)/admin/challenges/[id]/grading/page.tsx
autonomous: false
requirements:
  - APPR-08

must_haves:
  truths:
    - "Leader sees Submit for Review button when all essays graded"
    - "Leader can submit individual graded submissions"
    - "Leader can submit all graded submissions in bulk"
    - "Button shows Submitted state after submission"
    - "Notification triggered to Managers on submit"
  artifacts:
    - path: apps/web/app/components/admin/approvals/SubmitForReviewButton.tsx
      provides: Reusable submit button component
      exports: ["SubmitForReviewButton"]
    - path: apps/web/app/(protected)/admin/challenges/[id]/grading/page.tsx
      provides: Leader grading interface with submit buttons
      contains: "SubmitForReviewButton"
  key_links:
    - from: SubmitForReviewButton
      to: /api/challenges/submissions/[id]/submit-for-review
      via: fetch POST call
      pattern: "fetch.*submit-for-review"
    - from: grading page
      to: SubmitForReviewButton
      via: Component import and rendering
      pattern: "import.*SubmitForReviewButton"
---

<objective>
Create Leader submit UI components: SubmitForReviewButton component for individual submission submit, and bulk submit integration in grading interface.

Purpose: Enable Leaders to submit graded submissions for Manager review with individual and bulk submit capabilities, visual feedback, and Manager notifications.
Output: Submit button component with loading/disabled states, integrated into existing grading interface.
</objective>

<execution_context>
@/Users/nguyenthuylinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nguyenthuylinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-manager-approval-workflow/03-CONTEXT.md
@.planning/phases/03-manager-approval-workflow/03-RESEARCH.md
@.planning/phases/03-manager-approval-workflow/03-UI-SPEC.md
@.planning/phases/03-manager-approval-workflow/03-02-SUMMARY.md

@apps/web/app/(protected)/admin/challenges/[id]/grading/page.tsx
@apps/web/app/components/ui/button.tsx
</context>

<interfaces>
<!-- Key types and contracts from existing codebase -->

From database.types.ts (after Plan 03-01):
```typescript
type SubmissionStatus = 'in_progress' | 'submitted' | 'grading' | 'pending_review' | 'approved' | 'published'
```

From UI-SPEC.md button behavior:
```
- Only appears when all essay questions in submission are graded
- On click: Updates status to `pending_review`, creates approval record, notifies Manager
- After submit: Button becomes "Submitted" (disabled), shows "Submitted at [time]"
```

From UI-SPEC.md copywriting:
```
Primary CTA (Leader): Submit for Review
Toast on submit: "Submission sent to Manager for review"
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create SubmitForReviewButton component</name>
  <files>apps/web/app/components/admin/approvals/SubmitForReviewButton.tsx</files>
  <read_first>
    apps/web/app/components/ui/button.tsx
    apps/web/app/components/ui/badge.tsx
  </read_first>
  <action>
    Create SubmitForReviewButton component at apps/web/app/components/admin/approvals/SubmitForReviewButton.tsx:

    1. **Component interface:**
       - interface SubmitForReviewButtonProps { submissionId: string; allEssaysGraded: boolean; currentStatus: SubmissionStatus; onSubmitted?: () => void }

    2. **State management:**
       - const [isSubmitting, setIsSubmitting] = useState(false)
       - const [submittedAt, setSubmittedAt] = useState<Date | null>(null)

    3. **Determine button state:**
       - if currentStatus === 'pending_review' or currentStatus === 'approved' or currentStatus === 'published': show disabled "Submitted" button
       - if !allEssaysGraded: don't render button (or render disabled with tooltip)
       - if currentStatus === 'grading': show active "Submit for Review" button

    4. **Handle submit:**
       - async function handleSubmit()
       - setIsSubmitting(true)
       - fetch POST /api/challenges/submissions/${submissionId}/submit-for-review
       - On success: setSubmittedAt(new Date()), call onSubmitted?.(), show toast "Submission sent to Manager for review"
       - On error: show error toast
       - setIsSubmitting(false)

    5. **Render button:**
       - Use Button component from @/components/ui/button
       - Variant: "default" for active, "outline" for submitted/disabled
       - Show loading spinner when isSubmitting
       - Show "Submitted at {time}" when submitted
       - For Manager/Admin users: don't render button (Leaders only)

    6. **Use toast for notifications:**
       - Import toast from @/components/ui/use-toast (or similar existing toast system)
       - Show success/error toasts on submit completion

    Follow existing button component patterns. Use Tailwind classes: bg-[#1565C0] for primary action, disabled states for submitted.
  </action>
  <verify>
    <automated>grep -q "SubmitForReviewButton" apps/web/app/components/admin/approvals/SubmitForReviewButton.tsx && grep -q "submit-for-review" apps/web/app/components/admin/approvals/SubmitForReviewButton.tsx</automated>
  </verify>
  <done>
    SubmitForReviewButton component exists with props for submissionId, allEssaysGraded, currentStatus; handles submit API call with loading state; shows Submitted state after success; displays toast notifications; hides for non-Leader users.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create barrel export for approval components</name>
  <files>apps/web/app/components/admin/approvals/index.ts</files>
  <read_first>
    apps/web/app/components/admin/approvals/SubmitForReviewButton.tsx
  </read_first>
  <action>
    Create barrel export at apps/web/app/components/admin/approvals/index.ts:

    1. **Export SubmitForReviewButton:**
       - export { SubmitForReviewButton } from './SubmitForReviewButton'
       - export type { SubmitForReviewButtonProps } from './SubmitForReviewButton'

    This makes it easy to import approval components: import { SubmitForReviewButton } from '@/app/components/admin/approvals'
  </action>
  <verify>
    <automated>grep -q "SubmitForReviewButton" apps/web/app/components/admin/approvals/index.ts</automated>
  </verify>
  <done>
    Barrel export file exists exporting SubmitForReviewButton component and types for easy importing.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Integrate submit buttons into grading interface</name>
  <files>apps/web/app/(protected)/admin/challenges/[id]/grading/page.tsx</files>
  <read_first>
    apps/web/app/(protected)/admin/challenges/[id]/grading/page.tsx
  </read_first>
  <action>
    Modify the grading page to include submit buttons:

    1. **Import SubmitForReviewButton:**
       - import { SubmitForReviewButton } from '@/app/components/admin/approvals'

    2. **Add bulk submit button:**
       - Add "Submit All for Review" button near top of page (in header or toolbar area)
       - Button should: iterate through all submissions with status='grading' where all essays are graded, call submit API for each
       - Show loading state during bulk submit
       - Disable if no submissions ready to submit
       - Show toast: "Sent {count} submissions to Manager for review" on completion

    3. **Add individual submit buttons per submission:**
       - In the submission list/card/table, add SubmitForReviewButton for each submission
       - Pass props: submissionId, allEssaysGraded (check if all essay questions have manual_score), currentStatus
       - Position button near existing grade inputs or in submission actions area

    4. **Refresh data after submit:**
       - Call onSubmitted callback to refresh submission list from server
       - Use TanStack Query's invalidateQueries or refetch if available
       - Or manually re-fetch submissions after submit

    5. **Add status badges:**
       - Display submission status with Badge component (graded, pending_review, approved, published)
       - Use colors from UI-SPEC: gray for graded, amber for pending_review, emerald for approved, blue for published

    6. **Leader-only visibility:**
       - Only show submit buttons for users with role='leader'
       - Hide submit buttons for Manager/Admin (they can approve, not submit)

    Follow existing page structure and patterns. Use existing table/card layout for submissions.
  </action>
  <verify>
    <automated>grep -q "SubmitForReviewButton" apps/web/app/\(protected\)/admin/challenges/\[id\]/grading/page.tsx && grep -q "Submit All" apps/web/app/\(protected\)/admin/challenges/\[id\]/grading/page.tsx</automated>
  </verify>
  <done>
    Grading page includes SubmitForReviewButton for each submission, bulk "Submit All for Review" button at top, status badges showing submission state, data refresh after submit, and Leader-only visibility for submit buttons.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. SubmitForReviewButton component renders correctly in all states
2. Individual submit buttons appear next to each submission
3. Bulk submit button appears at top of grading page
4. Buttons show proper loading/disabled states
5. Toast notifications appear on submit success/error
6. Submission status updates correctly after submit
7. Status badges reflect current submission state
8. Submit buttons only visible for Leader role
</verification>

<success_criteria>
1. Leader can submit individual graded submissions
2. Leader can submit all graded submissions in bulk
3. Buttons show Submitted state after submission
4. Status badges display current submission state
5. Managers receive notifications when Leader submits
6. Submit actions only available to Leaders
</success_criteria>

<output>
After completion, create `.planning/phases/03-manager-approval-workflow/03-04-SUMMARY.md` with:
- SubmitForReviewButton component details
- Bulk submit implementation
- Status badge colors and labels
- Integration points with grading interface
- Role-based visibility rules
</output>
