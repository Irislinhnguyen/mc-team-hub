---
phase: 03-manager-approval-workflow
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - apps/web/app/api/challenges/submissions/[id]/submit-for-review/route.ts
  - apps/web/app/api/approvals/pending/route.ts
  - apps/web/app/api/approvals/submission/[id]/route.ts
autonomous: true
requirements:
  - APPR-04
  - APPR-07
  - APPR-14

must_haves:
  truths:
    - "Leader can submit graded submission for Manager review"
    - "Manager can list all pending approvals"
    - "Manager can view approval history for a submission"
    - "Notification sent to Managers when Leader submits"
  artifacts:
    - path: apps/web/app/api/challenges/submissions/[id]/submit-for-review/route.ts
      provides: POST endpoint for Leader to submit grades for review
      exports: ["POST"]
    - path: apps/web/app/api/approvals/pending/route.ts
      provides: GET endpoint for Manager to list pending approvals
      exports: ["GET"]
    - path: apps/web/app/api/approvals/submission/[id]/route.ts
      provides: GET endpoint for approval history
      exports: ["GET"]
  key_links:
    - from: submit-for-review API
      to: workflowNotificationService
      via: notifyManagersGradesSubmitted call
      pattern: "notifyManagersGradesSubmitted\\("
    - from: pending API
      to: challenge_submissions table
      via: Filter by status = 'pending_review'
      pattern: "status.*eq.*pending_review"
    - from: submission history API
      to: approvals table
      via: Join by submission_id
      pattern: "approvals.*submission_id"
---

<objective>
Create API endpoints for Leader submit workflow and Manager approval queue: POST endpoint for Leader to submit grades for review, GET endpoint for Manager to list pending approvals, and GET endpoint for approval history.

Purpose: Enable Leaders to submit graded submissions for Manager review and enable Managers to view pending approval queue with history.
Output: Three API endpoints with authentication, role-based access control, and notification integration.
</objective>

<execution_context>
@/Users/nguyenthuylinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nguyenthuylinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-manager-approval-workflow/03-CONTEXT.md
@.planning/phases/03-manager-approval-workflow/03-RESEARCH.md
@.planning/phases/03-manager-approval-workflow/03-01-SUMMARY.md

@apps/web/app/api/challenges/[id]/grading/route.ts
@apps/web/lib/services/workflowNotificationService.ts
@packages/auth/server.ts
</context>

<interfaces>
<!-- Key types and contracts from existing codebase -->

From packages/auth/server.ts:
```typescript
export async function getServerUser(): Promise<TokenPayload | null>
export function isLeaderOrAbove(user: TokenPayload): boolean
export function isAdminOrManager(user: TokenPayload): boolean
```

From apps/web/lib/services/workflowNotificationService.ts:
```typescript
export async function notifyManagersGradesSubmitted(
  challengeId: string,
  challengeName: string,
  leaderName: string,
  count: number
): Promise<void>
```

From database.types.ts (after Plan 03-01):
```typescript
type SubmissionStatus = 'in_progress' | 'submitted' | 'grading' | 'pending_review' | 'approved' | 'published'

interface Approval {
  id: string
  submission_id: string
  user_id: string
  user_role: 'leader' | 'manager' | 'admin'
  action: 'submitted_for_review' | 'approved'
  from_status: string
  to_status: string
  notes: string | null
  created_at: string
}

interface ChallengeSubmission {
  id: string
  status: SubmissionStatus
  // ... other fields
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Leader submit-for-review API endpoint</name>
  <files>apps/web/app/api/challenges/submissions/[id]/submit-for-review/route.ts</files>
  <read_first>
    apps/web/app/api/challenges/[id]/grading/route.ts
    packages/auth/server.ts
    apps/web/lib/services/workflowNotificationService.ts
  </read_first>
  <behavior>
    - Test 1: POST /api/challenges/submissions/{id}/submit-for-review with Leader auth returns 200 and updates status to pending_review
    - Test 2: POST with non-Leader auth returns 403 Forbidden
    - Test 3: POST with submission not in 'grading' status returns 400 Invalid state
    - Test 4: Successful submit creates approval record with action='submitted_for_review'
    - Test 5: Successful submit triggers notification to Managers
  </behavior>
  <action>
    Create POST endpoint at apps/web/app/api/challenges/submissions/[id]/submit-for-review/route.ts:

    1. **Import dependencies:**
       - import { NextRequest, NextResponse } from 'next/server'
       - import { getServerUser, isLeaderOrAbove } from '@query-stream-ai/auth/server'
       - import { createAdminClient } from '@query-stream-ai/db/admin'
       - import { notifyManagersGradesSubmitted } from '@/lib/services/workflowNotificationService'

    2. **Extract submission ID from URL:**
       - const params = await context.params
       - const submissionId = params.id (Note: use context.params for Next.js 15 App Router)

    3. **Authentication and authorization:**
       - const user = await getServerUser()
       - if (!user || !isLeaderOrAbove(user)) return NextResponse.json({ error: 'Forbidden' }, { status: 403 })

    4. **Validate current submission state:**
       - Query submission: select id, status, challenge_id from challenge_submissions where id = submissionId
       - if (!submission) return 404 Not Found
       - if (submission.status !== 'grading') return 400 with error 'Invalid state transition'

    5. **Update submission status to pending_review:**
       - update challenge_submissions set status = 'pending_review' where id = submissionId and status = 'grading'
       - if no rows updated, return 400 (state changed concurrently)

    6. **Create approval audit record:**
       - insert into approvals (submission_id, user_id, user_role, action, from_status, to_status)
       - values: (submissionId, user.sub, user.role, 'submitted_for_review', 'grading', 'pending_review')

    7. **Query challenge for notification:**
       - select id, name from challenges where id = submission.challenge_id

    8. **Trigger notification to Managers:**
       - await notifyManagersGradesSubmitted(challenge.id, challenge.name, user.name || 'A Leader', 1)
       - Wrap in try-catch, log errors but don't fail request

    9. **Return response:**
       - { success: true, submissionId, status: 'pending_review' }

    Follow existing patterns from grading/route.ts for structure, error handling, and logging with [Submit for Review] prefix.
  </action>
  <verify>
    <automated>grep -q "submit-for-review" apps/web/app/api/challenges/submissions/[*]/route.ts 2>/dev/null || grep -q "pending_review" apps/web/app/api/challenges/submissions/*-for-review/route.ts 2>/dev/null || find apps/web/app/api/challenges/submissions -name "route.ts" -exec grep -l "pending_review" {} \;</automated>
  </verify>
  <done>
    POST endpoint exists at /api/challenges/submissions/[id]/submit-for-review, authenticates Leader+ users, validates submission is in 'grading' state, updates to 'pending_review', creates approval audit record, and triggers notification to Managers.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create pending approvals list API endpoint</name>
  <files>apps/web/app/api/approvals/pending/route.ts</files>
  <read_first>
    apps/web/app/api/challenges/[id]/grading/route.ts
    packages/auth/server.ts
  </read_first>
  <behavior>
    - Test 1: GET /api/approvals/pending with Manager auth returns 200 with pending submissions array
    - Test 2: GET with non-Manager auth returns 403 Forbidden
    - Test 3: Response includes submission id, challenge name, student name, team, status, submitted_at
    - Test 4: Query filters by challengeId param when provided
    - Test 5: Query filters by teamId param when provided
  </behavior>
  <action>
    Create GET endpoint at apps/web/app/api/approvals/pending/route.ts:

    1. **Import dependencies:**
       - import { NextRequest, NextResponse } from 'next/server'
       - import { getServerUser, isAdminOrManager } from '@query-stream-ai/auth/server'
       - import { createAdminClient } from '@query-stream-ai/db/admin'

    2. **Authentication and authorization:**
       - const user = await getServerUser()
       - if (!user || !isAdminOrManager(user)) return NextResponse.json({ error: 'Forbidden' }, { status: 403 })

    3. **Parse query parameters:**
       - const searchParams = request.nextUrl.searchParams
       - const challengeId = searchParams.get('challengeId')
       - const teamId = searchParams.get('teamId')
       - const leaderId = searchParams.get('leaderId')

    4. **Build query with filters:**
       - Select from: challenge_submissions
       - Join: challenges (for challenge name), users (for student name), user_team_assignments (for team), approvals (for who submitted)
       - Where: challenge_submissions.status = 'pending_review'
       - Apply optional filters: challengeId, teamId, leaderId (from approvals where action='submitted_for_review')
       - Order by: challenge_submissions.updated_at DESC

    5. **Execute query:**
       - const { data: submissions, error } = await supabase.from('challenge_submissions').select(`
           id,
           status,
           created_at,
           updated_at,
           challenge_id,
           user_id,
           challenges!inner(id, name),
           users!inner(id, name, email),
           user_team_assignments!inner(team_id)
         `).eq('status', 'pending_review')

    6. **Return response:**
       - { submissions: [...], total: count }
       - Each submission includes: id, challenge.name, user.name, user.email, team_id, status, created_at, updated_at

    Add logging with [Pending Approvals] prefix. Handle errors with proper HTTP status codes.
  </action>
  <verify>
    <automated>grep -q "pending" apps/web/app/api/approvals/pending/route.ts && grep -q "isAdminOrManager" apps/web/app/api/approvals/pending/route.ts</automated>
  </verify>
  <done>
    GET endpoint exists at /api/approvals/pending, authenticates Manager/Admin users, returns list of submissions with status='pending_review', supports filtering by challengeId/teamId/leaderId, and includes related challenge, user, and team data.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create approval history API endpoint</name>
  <files>apps/web/app/api/approvals/submission/[id]/route.ts</files>
  <read_first>
    apps/web/app/api/approvals/pending/route.ts
    packages/auth/server.ts
  </read_first>
  <behavior>
    - Test 1: GET /api/approvals/submission/{id} with Manager auth returns 200 with approvals array
    - Test 2: GET with non-Manager auth returns 403 Forbidden
    - Test 3: Response includes all approval records for the submission
    - Test 4: Each approval includes action, user name, user role, from_status, to_status, created_at
  </behavior>
  <action>
    Create GET endpoint at apps/web/app/api/approvals/submission/[id]/route.ts:

    1. **Import dependencies:**
       - import { NextRequest, NextResponse } from 'next/server'
       - import { getServerUser, isAdminOrManager } from '@query-stream-ai/auth/server'
       - import { createAdminClient } from '@query-stream-ai/db/admin'

    2. **Extract submission ID from URL:**
       - const params = await context.params
       - const submissionId = params.id

    3. **Authentication and authorization:**
       - const user = await getServerUser()
       - if (!user || !isAdminOrManager(user)) return NextResponse.json({ error: 'Forbidden' }, { status: 403 })

    4. **Query approval history:**
       - Select from: approvals
       - Join: users (for user name who performed action)
       - Where: approvals.submission_id = submissionId
       - Order by: approvals.created_at ASC (chronological)

    5. **Execute query:**
       - const { data: approvals, error } = await supabase
           .from('approvals')
           .select(`
             id,
             action,
             from_status,
             to_status,
             notes,
             created_at,
             user_id,
             user_role,
             users!inner(id, name, email)
           `)
           .eq('submission_id', submissionId)
           .order('created_at', { ascending: true })

    6. **Return response:**
       - { approvals: [...], total: approvals.length }
       - Each approval includes: id, action, from_status, to_status, notes, created_at, user (id, name, email), user_role

    Add logging with [Approval History] prefix. Handle 404 if submission not found.
  </action>
  <verify>
    <automated>grep -q "approvals" apps/web/app/api/approvals/submission/*/route.ts && grep -q "submission_id" apps/web/app/api/approvals/submission/*/route.ts</automated>
  </verify>
  <done>
    GET endpoint exists at /api/approvals/submission/[id], authenticates Manager/Admin users, returns complete approval history for a submission in chronological order with user details for each approval action.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. All three API endpoints exist and are accessible
2. Leader submit endpoint properly validates state and creates audit records
3. Pending approvals endpoint returns properly filtered submissions
4. Approval history endpoint returns complete audit trail
5. Notifications are triggered when Leader submits for review
6. All endpoints have proper authentication and role-based access control
</verification>

<success_criteria>
1. POST /api/challenges/submissions/[id]/submit-for-review updates submission to pending_review
2. Approval audit record created on submit
3. Managers notified when Leader submits
4. GET /api/approvals/pending returns list of pending submissions
5. GET /api/approvals/submission/[id] returns approval history
6. All endpoints enforce proper role-based access
</success_criteria>

<output>
After completion, create `.planning/phases/03-manager-approval-workflow/03-02-SUMMARY.md` with:
- API endpoint details (routes, methods, authentication)
- Query patterns for pending approvals and history
- Notification integration points
- Role-based access control implementation
</output>
