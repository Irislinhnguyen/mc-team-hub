---
phase: 01-foundation-admin-unification
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - apps/admin/ (directory deleted)
  - apps/web/.env.local.example
  - apps/web/package.json
  - package.json
  - turbo.json
  - vercel.json
  - .env.local.example
autonomous: false
requirements:
  - ADM-04
  - ADM-05
user_setup: []

must_haves:
  truths:
    - "apps/admin directory no longer exists"
    - "Single Vercel project configuration"
    - "No references to @query-stream-ai/ui/* imports remain"
    - "Environment variables merged into apps/web/.env.local"
    - "Build succeeds without apps/admin"
    - "Admin routes still work at /admin/* URLs"
  artifacts:
    - path: "apps/admin/"
      provides: "DELETED - duplicate admin app"
      contains: "Directory should not exist after completion"
    - path: "apps/web/.env.local.example"
      provides: "Merged environment configuration"
      contains: "All env vars from apps/admin/.env.local"
    - path: ".env.local.example"
      provides: "Root environment example"
      contains: "All env vars needed for admin features"
  key_links:
    - from: "package.json"
      to: "apps/admin"
      via: "workspace configuration removal"
      pattern: "NOT IN 'workspaces'"
    - from: "turbo.json"
      to: "apps/admin"
      via: "pipeline configuration removal"
      pattern: "NOT IN 'pipeline'"
    - from: "vercel.json"
      to: "apps/admin"
      via: "project removal"
      pattern: "NOT IN 'projects'"
---

<objective>
Delete duplicate apps/admin directory and consolidate all configuration into single app deployment.

Purpose: Complete the consolidation by removing the duplicate admin app entirely. Merge environment configurations, remove workspace references from package.json/turbo.json, update Vercel configuration to deploy only apps/web. This eliminates code duplication, reduces deployment complexity, and cuts costs in half.

Output: apps/admin directory deleted, all env vars merged, build succeeds with single app, admin routes still functional at /admin/* URLs.
</objective>

<execution_context>
@/Users/nguyenthuylinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nguyenthuylinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-admin-unification/01-CONTEXT.md
@.planning/phases/01-foundation-admin-unification/01-RESEARCH.md

# Files to modify for consolidation
@/Users/nguyenthuylinh/Downloads/query-stream-ai/package.json
@/Users/nguyenthuylinh/Downloads/query-stream-ai/turbo.json
@/Users/nguyenthuylinh/Downloads/query-stream-ai/vercel.json
@/Users/nguyenthuylinh/Downloads/query-stream-ai/apps/admin/.env.local.example
@/Users/nguyenthuylinh/Downloads/query-stream-ai/apps/web/.env.local.example
@/Users/nguyenthuylinh/Downloads/query-stream-ai/.env.local.example

# Directory to delete
@/Users/nguyenthuylinh/Downloads/query-stream-ai/apps/admin/

# Previous plan outputs
@/Users/nguyenthuylinh/Downloads/query-stream-ai/.planning/phases/01-foundation-admin-unification/01-01-SUMMARY.md
@/Users/nguyenthuylinh/Downloads/query-stream-ai/.planning/phases/01-foundation-admin-unification/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge environment configurations</name>
  <files>apps/web/.env.local.example, .env.local.example</files>
  <read_first>
    1. apps/admin/.env.local.example — admin app environment variables
    2. apps/web/.env.local.example — web app environment variables
    3. .env.local.example — root environment variables
  </read_first>
  <action>
Merge environment configurations from apps/admin into apps/web and root:

1. Read apps/admin/.env.local.example and apps/web/.env.local.example

2. For each env var in apps/admin/.env.local.example:
   - If it does NOT exist in apps/web/.env.local.example, add it
   - If it exists with different value/default, merge appropriately

3. Typical env vars to check (from CONTEXT.md):
   - NEXT_PUBLIC_SUPABASE_URL
   - NEXT_PUBLIC_SUPABASE_ANON_KEY
   - SUPABASE_SERVICE_ROLE_KEY
   - NEXT_PUBLIC_APP_URL
   - Any admin-specific vars (e.g., EMAIL_* for notifications in Phase 2)

4. Update apps/web/.env.local.example with merged vars

5. Also update root .env.local.example if it exists

6. Add comment at top of apps/web/.env.local.example:
   ```bash
   # Environment variables for Query Stream AI
   # This is the single app configuration (formerly apps/web and apps/admin merged)
   ```

DO NOT modify actual .env.local files (only .example files). Actual env vars are managed outside version control.
  </action>
  <verify>
    <automated>grep -c "NEXT_PUBLIC_" apps/web/.env.local.example && grep -c "SUPABASE_" apps/web/.env.local.example</automated>
  </verify>
  <done>
    Environment variables merged:
    - apps/admin/.env.local.example vars added to apps/web/.env.local.example
    - Root .env.local.example updated if needed
    - No duplicate env vars
    - Comments explaining merged configuration
    - All env vars from both apps present in apps/web/.env.local.example
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove apps/admin from package.json workspaces</name>
  <files>package.json</files>
  <read_first>
    1. package.json — root package.json with workspaces configuration
    2. apps/admin/package.json — admin app package.json to check dependencies
    3. apps/web/package.json — web app package.json to verify dependencies exist
  </read_first>
  <action>
Update package.json to remove apps/admin from monorepo:

1. Read root package.json

2. Remove 'apps/admin' from workspaces array:
   - If workspaces is: ["apps/*", "packages/*"], change to ["apps/web", "packages/*"]
   - Or if explicit: ["apps/web", "packages/*", "apps/admin"], remove "apps/admin"

3. Check if apps/admin has any unique dependencies not in apps/web/package.json:
   - Read both package.json files
   - If apps/admin has dependencies not in apps/web, add them to apps/web/package.json
   - Common dependencies to check: next, react, react-dom, @tanstack/react-query, @radix-ui/*, lucide-react, etc.

4. Note: Most dependencies should already be in apps/web since they're 99% duplicated

5. Keep apps/web in workspaces

6. Keep packages/* workspaces (packages/ui, packages/auth, packages/db) unchanged

DO NOT remove packages/* workspaces or apps/web from workspaces.
  </action>
  <verify>
    <automated>grep -v "apps/admin" package.json | grep -c "workspaces"</automated>
  </verify>
  <done>
    Root package.json updated:
    - apps/admin removed from workspaces array
    - apps/web remains in workspaces
    - packages/* workspaces unchanged
    - Any unique dependencies from apps/admin added to apps/web/package.json
    - Workspaces now only include: apps/web, packages/ui, packages/auth, packages/db
  </done>
</task>

<task type="auto">
  <name>Task 3: Update turbo.json to remove apps/admin pipeline</name>
  <files>turbo.json</files>
  <read_first>
    1. turbo.json — Turbo configuration with pipeline tasks
  </read_first>
  <action>
Update turbo.json to remove apps/admin references:

1. Read turbo.json

2. If pipeline has explicit app entries:
   - Remove 'admin' entry from pipeline object
   - Keep 'web' entry unchanged

3. If pipeline uses wildcards like "apps/*":
   - Change to explicit apps/web reference
   - Or add filter to exclude apps/admin

4. Typical turbo.json structure:
   ```json
   {
     "pipeline": {
       "web": { "dependsOn": ["^build"], "outputs": [".next/**"] },
       "admin": { ... } // REMOVE THIS
     }
   }
   ```

5. Ensure apps/web pipeline is correct

6. Keep packages/* pipelines unchanged

DO NOT modify apps/web pipeline configuration. DO NOT remove packages/* pipelines.
  </action>
  <verify>
    <automated>grep -v "\"admin\"" turbo.json | grep -c "\"pipeline\""</automated>
  </verify>
  <done>
    turbo.json updated:
    - 'admin' pipeline entry removed
    - 'web' pipeline unchanged
    - packages/* pipelines unchanged
    - No references to apps/admin remain
    - Build will only run for apps/web and packages/*
  </done>
</task>

<task type="auto">
  <name>Task 4: Update vercel.json for single project deployment</name>
  <files>vercel.json</files>
  <read_first>
    1. vercel.json — Vercel deployment configuration
  </read_first>
  <action>
Update vercel.json to deploy only apps/web:

1. Read vercel.json

2. If projects array includes both apps:
   - Remove admin project entry
   - Keep web project entry

3. If using workspace roots pattern:
   - Remove admin from workspace list
   - Keep web workspace

4. If using framework field:
   - Ensure it points to apps/web
   - Remove any admin-specific configuration

5. If using buildCommand:
   - Ensure it doesn't reference apps/admin
   - Change from "turbo run build" to "turbo run build --filter=web..." if needed

6. Typical vercel.json structure after update:
   ```json
   {
     "buildCommand": "turbo run build --filter=web...",
     "framework": "nextjs",
     "outputDirectory": "apps/web/.next"
   }
   ```

DO NOT remove apps/web configuration. DO NOT modify Vercel environment variables (those are managed in Vercel dashboard).
  </action>
  <verify>
    <automated>grep -v "admin" vercel.json | grep -c "buildCommand"</automated>
  </verify>
  <done>
    vercel.json updated:
    - Admin project/workspace removed
    - Web configuration unchanged
    - Build command only references apps/web
    - No references to apps/admin remain
    - Single Vercel project configuration
  </done>
</task>

<task type="auto">
  <name>Task 5: Verify no remaining @query-stream-ai/ui imports</name>
  <files>apps/web/app/**/*.tsx, apps/web/app/**/*.ts</files>
  <read_first>
    1. None (source analysis task)
  </read_first>
  <action>
Search for and eliminate any remaining @query-stream-ai/ui imports in apps/web:

1. Search for @query-stream-ai/ui imports in apps/web:
   ```bash
   grep -r "@query-stream-ai/ui" apps/web/app/
   grep -r "@query-stream-ai/ui" apps/web/lib/
   ```

2. If any files still use @query-stream-ai/ui imports:
   - Replace with @/components/ui/* equivalents
   - For example: @query-stream-ai/ui/badge → @/components/ui/badge
   - For example: @query-stream-ai/ui/button → @/components/ui/button

3. Common imports to check:
   - @query-stream-ai/ui/badge → @/components/ui/badge
   - @query-stream-ai/ui/button → @/components/ui/button
   - @query-stream-ai/ui/card → @/components/ui/card
   - @query-stream-ai/ui/dialog → @/components/ui/dialog
   - @query-stream-ai/ui/form → @/components/ui/form
   - @query-stream-ai/ui/table → @/components/ui/table

4. Verify all component files use local imports

5. If no @query-stream-ai/ui imports found, task is complete

DO NOT change @query-stream-ai/auth, @query-stream-ai/db, or other package imports. Only change @query-stream-ai/ui imports.
  </action>
  <verify>
    <automated>! grep -r "@query-stream-ai/ui" apps/web/app/ || ! grep -r "@query-stream-ai/ui" apps/web/lib/ || echo "No @query-stream-ai/ui imports found - PASS"</automated>
  </verify>
  <done>
    All @query-stream-ai/ui imports replaced:
    - No files in apps/web/app/ use @query-stream-ai/ui imports
    - No files in apps/web/lib/ use @query-stream-ai/ui imports
    - All UI components imported from @/components/ui/*
    - Other package imports (@query-stream-ai/auth, @query-stream-ai/db) unchanged
  </done>
</task>

<task type="auto">
  <name>Task 6: Delete apps/admin directory</name>
  <files>apps/admin/</files>
  <read_first>
    1. None (deletion task)
  </read_first>
  <action>
Delete the duplicate apps/admin directory:

1. Verify all previous tasks complete:
   - Environment configs merged
   - package.json workspaces updated
   - turbo.json pipeline updated
   - vercel.json updated
   - No @query-stream-ai/ui imports remain

2. Delete apps/admin directory:
   ```bash
   rm -rf apps/admin
   ```

3. Verify deletion:
   ```bash
   ls -la apps/
   ```
   Should show only: web

4. Check git status to confirm deletion

5. No backup needed — git history preserves files if needed

DO NOT delete apps/web, packages/ui, packages/auth, packages/db directories.
  </action>
  <verify>
    <automated>! test -d apps/admin && echo "apps/admin directory deleted - PASS" || echo "apps/admin still exists - FAIL"</automated>
  </verify>
  <done>
    apps/admin directory deleted:
    - apps/admin no longer exists on filesystem
    - Only apps/web remains in apps/ directory
    - Git shows deletion of apps/admin files
    - No backup needed (git history preserves if needed)
  </done>
</task>

<task type="auto">
  <name>Task 7: Verify build succeeds with single app</name>
  <files>apps/web/</files>
  <read_first>
    1. package.json — verify build scripts
    2. turbo.json — verify pipeline configuration
  </read_first>
  <action>
Verify build succeeds after consolidation:

1. Clean build artifacts:
   ```bash
   rm -rf apps/web/.next
   rm -rf node_modules/.cache
   ```

2. Install dependencies (if needed):
   ```bash
   pnpm install
   ```

3. Build apps/web:
   ```bash
   pnpm turbo run build --filter=web
   ```

4. Verify build succeeds without errors:
   - No TypeScript errors
   - No import errors
   - No missing dependencies
   - .next directory created in apps/web/

5. Check for any apps/admin references in build output

6. Run TypeScript check:
   ```bash
   cd apps/web && npx tsc --noEmit
   ```

7. If build fails:
   - Fix import errors
   - Add missing dependencies
   - Update configuration
   - Re-run build

DO NOT proceed to next task if build fails.
  </action>
  <verify>
    <automated>test -d apps/web/.next && echo "Build succeeded - .next exists" || echo "Build failed - no .next directory"</automated>
  </verify>
  <done>
    Build verification complete:
    - apps/web builds successfully
    - No TypeScript errors
    - No import errors
    - No references to apps/admin in build output
    - .next directory created successfully
    - Ready for deployment
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 03 completes the admin consolidation:
    - Environment variables merged into apps/web/.env.local.example
    - apps/admin removed from package.json workspaces
    - turbo.json pipeline updated (only apps/web)
    - vercel.json configured for single project
    - All @query-stream-ai/ui imports replaced with @/components/ui/*
    - apps/admin directory deleted
    - Build succeeds with single app configuration

    The project now has a single admin interface at /admin/* routes with unified components and layout.
  </what-built>
  <how-to-verify>
    1. Verify directory structure:
       ```bash
       ls -la apps/
       ```
       Should show only: web

    2. Verify no apps/admin references:
       ```bash
       grep -r "apps/admin" package.json turbo.json vercel.json
       ```
       Should return nothing

    3. Verify build succeeds:
       ```bash
       pnpm turbo run build --filter=web
       ```
       Should complete without errors

    4. Start dev server:
       ```bash
       cd apps/web && pnpm dev
       ```

    5. Visit admin routes and verify:
       - http://localhost:3000/admin/overview — loads
       - http://localhost:3000/admin/ai-usage — loads
       - http://localhost:3000/admin/challenges — loads
       - http://localhost:3000/admin/users — loads
       - http://localhost:3000/admin/feedback — loads
       - http://localhost:3000/admin/team-settings — loads

    6. Check Vercel configuration:
       - vercel.json references only apps/web
       - Single project configured

    7. Verify git status shows apps/admin deletion
  </how-to-verify>
  <resume-signal>Type "approved" if consolidation complete and all admin routes work, or describe issues</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. Verify apps/admin directory deleted: `! test -d apps/admin`
2. Verify no @query-stream-ai/ui imports: `! grep -r "@query-stream-ai/ui" apps/web/`
3. Verify package.json workspaces: `grep -c "apps/admin" package.json` should return 0
4. Verify turbo.json: `grep -c "\"admin\"" turbo.json` should return 0
5. Verify build succeeds: `pnpm turbo run build --filter=web`
6. Verify admin routes work: Test each /admin/* URL in browser
</verification>

<success_criteria>
1. apps/admin directory deleted completely
2. Single Vercel project configuration (vercel.json)
3. No @query-stream-ai/ui imports remain in apps/web
4. Environment variables merged into apps/web/.env.local.example
5. Build succeeds with single app
6. All admin routes still functional at /admin/* URLs
7. ADM-04 satisfied: "Consolidate apps/admin into apps/web (eliminate duplicate app)"
8. ADM-05 satisfied: "Delete duplicate UI components and services"
9. Cost reduction: 50% (2 Vercel projects → 1 Vercel project)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-admin-unification/01-03-SUMMARY.md` with:
- Directory deleted
- Configuration files updated
- Build verification results
- Cost savings achieved
- Phase 1 complete summary
</output>
