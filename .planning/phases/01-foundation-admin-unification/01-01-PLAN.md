---
phase: 01-foundation-admin-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/components/admin/AdminTable.tsx
  - apps/web/app/components/admin/AdminForm.tsx
  - apps/web/app/components/admin/AdminDialog.tsx
  - apps/web/app/components/admin/AdminHeader.tsx
  - apps/web/app/components/admin/index.ts
autonomous: true
requirements:
  - ADM-03
user_setup: []

must_haves:
  truths:
    - "Admin components can be imported from @/components/admin"
    - "AdminTable renders data with shadcn/ui Table primitives"
    - "AdminForm wraps react-hook-form with validation"
    - "AdminDialog wraps shadcn/ui Dialog with consistent styling"
    - "AdminHeader provides consistent page headers"
    - "All components use @/components/ui/* imports (not @query-stream-ai/ui/*)"
  artifacts:
    - path: "apps/web/app/components/admin/AdminTable.tsx"
      provides: "Reusable data table for admin pages"
      min_lines: 40
      exports: ["AdminTable"]
    - path: "apps/web/app/components/admin/AdminForm.tsx"
      provides: "Form wrapper with react-hook-form integration"
      min_lines: 30
      exports: ["AdminForm"]
    - path: "apps/web/app/components/admin/AdminDialog.tsx"
      provides: "Dialog/modal wrapper with consistent styling"
      min_lines: 35
      exports: ["AdminDialog"]
    - path: "apps/web/app/components/admin/AdminHeader.tsx"
      provides: "Common header pattern for admin pages"
      min_lines: 25
      exports: ["AdminHeader"]
    - path: "apps/web/app/components/admin/index.ts"
      provides: "Barrel export for admin components"
      min_lines: 5
      exports: ["AdminTable", "AdminForm", "AdminDialog", "AdminHeader"]
  key_links:
    - from: "AdminTable.tsx"
      to: "@/components/ui/table"
      via: "import statement"
      pattern: "from '@/components/ui/table'"
    - from: "AdminForm.tsx"
      to: "react-hook-form"
      via: "import statement"
      pattern: "from 'react-hook-form'"
    - from: "AdminDialog.tsx"
      to: "@/components/ui/dialog"
      via: "import statement"
      pattern: "from '@/components/ui/dialog'"
---

<objective>
Create shared admin components that establish consistent patterns across all admin interfaces.

Purpose: Eliminate duplicate component code, provide reusable UI patterns for admin features (Bible, Challenges, Dashboard), ensure consistent styling and behavior across all admin pages.

Output: Four reusable admin components (AdminTable, AdminForm, AdminDialog, AdminHeader) with barrel export, all using standard @/components/ui/* imports.
</objective>

<execution_context>
@/Users/nguyenthuylinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nguyenthuylinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-admin-unification/01-CONTEXT.md
@.planning/phases/01-foundation-admin-unification/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

# Import standardization requirement
ALL components must use `@/components/ui/*` import pattern, NOT `@query-stream-ai/ui/*`. This is a locked decision from CONTEXT.md.

# Existing shadcn/ui components available
@/Users/nguyenthuylinh/Downloads/query-stream-ai/apps/web/app/components/ui/table.tsx
@/Users/nguyenthuylinh/Downloads/query-stream-ai/apps/web/app/components/ui/dialog.tsx
@/Users/nguyenthuylinh/Downloads/query-stream-ai/apps/web/app/components/ui/form.tsx

# Existing admin patterns to reference
@/Users/nguyenthuylinh/Downloads/query-stream-ai/apps/web/app/(protected)/admin/users/page.tsx
@/Users/nguyenthuylinh/Downloads/query-stream-ai/apps/web/app/(protected)/admin/challenges/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AdminTable component with shadcn/ui Table</name>
  <files>apps/web/app/components/admin/AdminTable.tsx</files>
  <read_first>
    1. apps/web/app/components/ui/table.tsx — to understand available Table exports (Table, TableHeader, TableBody, TableRow, TableCell, TableHead)
    2. apps/web/app/(protected)/admin/users/page.tsx — to see current table usage pattern
  </read_first>
  <action>
Create reusable AdminTable component at apps/web/app/components/admin/AdminTable.tsx:

1. Import Table primitives from @/components/ui/table (NOT @query-stream-ai/ui/table)
2. Define AdminTableProps interface with:
   - data: T[] — array of data items
   - columns: Column<T>[] — column definitions
   - onRowClick?: (row: T) => void — optional click handler
3. Column interface should have:
   - key: string — unique identifier
   - header: string — column header text
   - render: (row: T) => React.ReactNode — cell renderer
4. Return Table component with:
   - TableHeader with TableHead for each column
   - TableBody with TableRow for each data item
   - TableCell with column render function output
   - Optional: click handler on rows if onRowClick provided
5. Export as named export: export function AdminTable<T>({ data, columns, onRowClick }: AdminTableProps<T>)

Use exact imports: import { Table, TableHeader, TableBody, TableRow, TableCell, TableHead } from '@/components/ui/table'

DO NOT use @query-stream-ai/ui imports.
  </action>
  <verify>
    <automated>grep -n "from '@/components/ui/table'" apps/web/app/components/admin/AdminTable.tsx && grep -n "export function AdminTable" apps/web/app/components/admin/AdminTable.tsx</automated>
  </verify>
  <done>
    AdminTable.tsx exists with:
    - Imports from @/components/ui/table (not @query-stream-ai/ui)
    - Generic AdminTable component with data, columns, onRowClick props
    - Proper TypeScript types (AdminTableProps, Column interface)
    - Named export function
  </done>
</task>

<task type="auto">
  <name>Task 2: Create AdminForm component with react-hook-form</name>
  <files>apps/web/app/components/admin/AdminForm.tsx</files>
  <read_first>
    1. apps/web/app/components/ui/form.tsx — to understand Form component exports
    2. apps/web/app/components/ui/button.tsx — to see Button component API
    3. package.json — verify react-hook-form and @hookform/resolvers are installed
  </read_first>
  <action>
Create reusable AdminForm component at apps/web/app/components/admin/AdminForm.tsx:

1. Import from:
   - react-hook-form: useForm, UseFormReturn, FieldValues (already installed per package.json)
   - @/components/ui/form: Form, FormControl, FormField, FormItem, FormLabel, FormMessage
   - @/components/ui/button: Button
   - @/components/ui/input: Input (if needed for default inputs)

2. Define AdminFormProps interface:
   - form: UseFormReturn<TFieldValues> — react-hook-form instance
   - onSubmit: (data: TFieldValues) => void | Promise<void> — submit handler
   - children: React.ReactNode — form fields
   - submitLabel?: string — custom submit button text (default: "Submit")
   - isLoading?: boolean — loading state for submit button

3. Return Form component with:
   - Form wrapper from shadcn/ui
   - FormField patterns for child components
   - Submit button at bottom with:
     - isLoading prop showing loading spinner/text
     - type="submit"
     - Disabled when loading

4. Export as named export: export function AdminForm<TFieldValues extends FieldValues>({ form, onSubmit, children, submitLabel, isLoading }: AdminFormProps<TFieldValues>)

Use exact imports:
- import { UseFormReturn, FieldValues } from 'react-hook-form'
- import { Form } from '@/components/ui/form'
- import { Button } from '@/components/ui/button'

DO NOT use @query-stream-ai/ui imports.
  </action>
  <verify>
    <automated>grep -n "from '@/components/ui/form'" apps/web/app/components/admin/AdminForm.tsx && grep -n "from 'react-hook-form'" apps/web/app/components/admin/AdminForm.tsx</automated>
  </verify>
  <done>
    AdminForm.tsx exists with:
    - Imports from @/components/ui/form and react-hook-form (not @query-stream-ai/ui)
    - Generic AdminForm component with form, onSubmit, children props
    - Submit button with isLoading state
    - Proper TypeScript types extending FieldValues
    - Named export function
  </done>
</task>

<task type="auto">
  <name>Task 3: Create AdminDialog component with shadcn/ui Dialog</name>
  <files>apps/web/app/components/admin/AdminDialog.tsx</files>
  <read_first>
    1. apps/web/app/components/ui/dialog.tsx — to understand Dialog exports (Dialog, DialogTrigger, DialogContent, DialogHeader, DialogTitle, DialogFooter)
    2. apps/web/app/components/ui/button.tsx — for Button component API
  </read_first>
  <action>
Create reusable AdminDialog component at apps/web/app/components/admin/AdminDialog.tsx:

1. Import from:
   - @/components/ui/dialog: Dialog, DialogTrigger, DialogContent, DialogHeader, DialogTitle, DialogFooter
   - @/components/ui/button: Button

2. Define AdminDialogProps interface:
   - open: boolean — controlled open state
   - onOpenChange: (open: boolean) => void — open state changer
   - title: string — dialog title
   - children: React.ReactNode — dialog content
   - footer?: React.ReactNode — optional footer content
   - size?: 'sm' | 'md' | 'lg' | 'xl' — dialog size variant

3. Create size class mapping:
   - sm: 'max-w-sm'
   - md: 'max-w-md'
   - lg: 'max-w-lg'
   - xl: 'max-w-xl'

4. Return Dialog with:
   - Dialog root with open/onOpenChange
   - DialogContent with size class applied
   - DialogHeader with DialogTitle showing title prop
   - Children content
   - DialogFooter with optional footer content

5. Export as named export: export function AdminDialog({ open, onOpenChange, title, children, footer, size = 'md' }: AdminDialogProps)

Use exact imports: import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter } from '@/components/ui/dialog'

DO NOT use @query-stream-ai/ui imports.
  </action>
  <verify>
    <automated>grep -n "from '@/components/ui/dialog'" apps/web/app/components/admin/AdminDialog.tsx && grep -n "export function AdminDialog" apps/web/app/components/admin/AdminDialog.tsx</automated>
  </verify>
  <done>
    AdminDialog.tsx exists with:
    - Imports from @/components/ui/dialog (not @query-stream-ai/ui)
    - AdminDialog component with open, onOpenChange, title, children, footer, size props
    - Size variant classes (sm, md, lg, xl)
    - Proper TypeScript types
    - Named export function
  </done>
</task>

<task type="auto">
  <name>Task 4: Create AdminHeader component and barrel export</name>
  <files>apps/web/app/components/admin/AdminHeader.tsx, apps/web/app/components/admin/index.ts</files>
  <read_first>
    1. apps/web/app/components/ui/button.tsx — for Button component API
    2. apps/web/app/(protected)/admin/users/page.tsx — to see current header pattern (if any)
  </read_first>
  <action>
Create AdminHeader component and barrel export:

**Part A: Create AdminHeader.tsx**
At apps/web/app/components/admin/AdminHeader.tsx:

1. Import from:
   - @/components/ui/button: Button
   - lucide-react: Plus icon or similar

2. Define AdminHeaderProps interface:
   - title: string — page title
   - description?: string — optional page description
   - action?: { label: string; onClick: () => void; icon?: React.ReactNode } — optional action button

3. Return header div with:
   - Flex container (flex justify-between items-center)
   - Left side: title h1, optional description p
   - Right side: optional action button with icon

4. Export as named export: export function AdminHeader({ title, description, action }: AdminHeaderProps)

**Part B: Create index.ts barrel export**
At apps/web/app/components/admin/index.ts:

1. Export all admin components:
   - export { AdminTable } from './AdminTable'
   - export { AdminForm } from './AdminForm'
   - export { AdminDialog } from './AdminDialog'
   - export { AdminHeader } from './AdminHeader'

2. Export types if defined:
   - export type { AdminTableProps } from './AdminTable'
   - export type { AdminFormProps } from './AdminForm'
   - export type { AdminDialogProps } from './AdminDialog'
   - export type { AdminHeaderProps } from './AdminHeader'

Use exact imports: import { Button } from '@/components/ui/button'

DO NOT use @query-stream-ai/ui imports.
  </action>
  <verify>
    <automated>grep -n "export { AdminTable }" apps/web/app/components/admin/index.ts && grep -n "export { AdminForm }" apps/web/app/components/admin/index.ts && grep -n "export { AdminDialog }" apps/web/app/components/admin/index.ts && grep -n "export { AdminHeader }" apps/web/app/components/admin/index.ts</automated>
  </verify>
  <done>
    AdminHeader.tsx exists with:
    - Imports from @/components/ui/button (not @query-stream-ai/ui)
    - AdminHeader component with title, description, action props
    - Named export function

index.ts exists with:
    - All four components exported (AdminTable, AdminForm, AdminDialog, AdminHeader)
    - All types exported
    - Clean barrel exports from component files
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Verify all components use @/components/ui/* imports: `grep -r "@query-stream-ai/ui" apps/web/app/components/admin/` should return nothing
2. Verify all components are exported from index.ts
3. Verify TypeScript compiles: `npx tsc --noEmit` in apps/web directory
4. Test import works: Create test file with `import { AdminTable, AdminForm, AdminDialog, AdminHeader } from '@/components/admin'`
</verification>

<success_criteria>
1. Four reusable admin components exist (AdminTable, AdminForm, AdminDialog, AdminHeader)
2. All components use @/components/ui/* import pattern (not @query-stream-ai/ui/*)
3. Barrel export (index.ts) provides clean imports for downstream consumers
4. Components are ready for use in Phase 2 (notifications), Phase 3 (approval workflow), Phase 4 (dashboard), Phase 5 (Bible admin)
5. ADM-03 requirement satisfied: "Shared admin components (forms, tables, dialogs) for reuse"
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-admin-unification/01-01-SUMMARY.md` with:
- Components created
- Import patterns used
- Any deviations from plan
- Ready for next plan (consolidation)
</output>
