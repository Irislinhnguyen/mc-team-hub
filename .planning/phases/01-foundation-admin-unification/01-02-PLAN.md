---
phase: 01-foundation-admin-unification
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/web/app/(protected)/admin/layout.tsx
  - apps/web/app/(protected)/admin/AdminSidebar.tsx
  - apps/web/app/(protected)/admin/page.tsx
  - apps/web/app/(protected)/admin/overview/page.tsx
  - apps/web/middleware.ts
autonomous: false
requirements:
  - ADM-01
  - ADM-02
  - ADM-06
user_setup: []

must_haves:
  truths:
    - "Admin layout has unified AdminSidebar with all navigation items"
    - "Admin layout enforces role-based access (leader and above)"
    - "Admin routes work at /admin/* URLs"
    - "Middleware correctly redirects unauthorized users"
    - "AdminSidebar uses @/components/ui/* imports (not @query-stream-ai/ui/*)"
    - "Overview page placeholder exists for Phase 4 dashboard"
  artifacts:
    - path: "apps/web/app/(protected)/admin/layout.tsx"
      provides: "Unified admin layout with auth check"
      min_lines: 25
      exports: ["default"]
    - path: "apps/web/app/(protected)/admin/AdminSidebar.tsx"
      provides: "Unified sidebar with all admin navigation"
      min_lines: 80
      exports: ["default"]
    - path: "apps/web/app/(protected)/admin/overview/page.tsx"
      provides: "Placeholder for Phase 4 dashboard"
      min_lines: 15
      exports: ["default"]
  key_links:
    - from: "apps/web/app/(protected)/admin/layout.tsx"
      to: "@query-stream-ai/auth/server"
      via: "import statement"
      pattern: "from '@query-stream-ai/auth/server'"
    - from: "apps/web/app/(protected)/admin/AdminSidebar.tsx"
      to: "@/components/ui/badge"
      via: "import statement"
      pattern: "from '@/components/ui/badge'"
    - from: "apps/web/middleware.ts"
      to: "/admin/* routes"
      via: "route protection"
      pattern: "pathname\\.startsWith\\('/admin'\\)"
---

<objective>
Unify admin layout and sidebar with consistent navigation patterns and role-based access control.

Purpose: Consolidate duplicate admin layouts from apps/admin and apps/web into a single unified admin layout. Create consistent sidebar navigation that will serve all admin features (challenges, users, AI usage, feedback, team settings, Bible admin, overview dashboard). Ensure role-based access control is enforced.

Output: Unified admin layout at apps/web/app/(protected)/admin/layout.tsx, AdminSidebar with all navigation items and @/components/ui/* imports, overview page placeholder for Phase 4.
</objective>

<execution_context>
@/Users/nguyenthuylinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nguyenthuylinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-admin-unification/01-CONTEXT.md
@.planning/phases/01-foundation-admin-unification/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md

# Existing admin layouts to merge
@/Users/nguyenthuylinh/Downloads/query-stream-ai/apps/web/app/(protected)/admin/layout.tsx
@/Users/nguyenthuylinh/Downloads/query-stream-ai/apps/admin/app/admin/layout.tsx

# Existing admin sidebars to merge
@/Users/nguyenthuylinh/Downloads/query-stream-ai/apps/web/app/(protected)/admin/AdminSidebar.tsx
@/Users/nguyenthuylinh/Downloads/query-stream-ai/apps/admin/app/admin/AdminSidebar.tsx

# Existing admin pages
@/Users/nguyenthuylinh/Downloads/query-stream-ai/apps/web/app/(protected)/admin/page.tsx

# Shared admin components from Plan 01
@/Users/nguyenthuylinh/Downloads/query-stream-ai/apps/web/app/components/admin/index.ts

# Navigation items from CONTEXT.md
/navItems = [
  { name: 'Overview', href: '/admin/overview', icon: <Home /> },           // Phase 4: Dashboard
  { name: 'AI Usage', href: '/admin/ai-usage', icon: <BarChart3 /> },       // Existing
  { name: 'Challenges', href: '/admin/challenges', icon: <Trophy /> },      // Existing
  { name: 'Bible', href: '/admin/bible', icon: <BookOpen /> },              // Phase 5: Bible admin (NEW)
  { name: 'Users', href: '/admin/users', icon: <Users /> },                 // Existing
  { name: 'Feedback', href: '/admin/feedback', icon: <MessageSquare /> },   // Existing
  { name: 'Team Settings', href: '/admin/team-settings', icon: <Settings /> }, // Existing
]
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AdminSidebar with unified navigation and standard imports</name>
  <files>apps/web/app/(protected)/admin/AdminSidebar.tsx</files>
  <read_first>
    1. apps/web/app/(protected)/admin/AdminSidebar.tsx — current web app sidebar (uses @/components/ui/badge)
    2. apps/admin/app/admin/AdminSidebar.tsx — admin app sidebar (uses @query-stream-ai/ui/badge)
    3. .planning/phases/01-foundation-admin-unification/01-CONTEXT.md — navigation requirements
  </read_first>
  <action>
Update AdminSidebar at apps/web/app/(protected)/admin/AdminSidebar.tsx to have unified navigation:

1. Change import from `@query-stream-ai/ui/badge` to `@/components/ui/badge` (already correct in web app version, keep this)

2. Update navItems array to include all admin navigation items:
   ```typescript
   const navItems = [
     { name: 'Overview', href: '/admin/overview', icon: <Home size={18} /> },
     { name: 'AI Usage', href: '/admin/ai-usage', icon: <BarChart3 size={18} /> },
     { name: 'Challenges', href: '/admin/challenges', icon: <Trophy size={18} /> },
     { name: 'Bible', href: '/admin/bible', icon: <BookOpen size={18} /> },
     { name: 'Users', href: '/admin/users', icon: <Users size={18} /> },
     { name: 'Feedback', href: '/admin/feedback', icon: <MessageSquare size={18} /> },
     { name: 'Team Settings', href: '/admin/team-settings', icon: <Settings size={18} /> },
   ]
   ```

3. Add BookOpen icon import: import { BookOpen } from 'lucide-react'

4. Keep existing role badge styles and getRoleBadgeStyle function

5. Keep existing layout structure (header, navigation, footer)

6. Ensure import is: import { Badge } from '@/components/ui/badge' (NOT @query-stream-ai/ui/badge)

DO NOT change any other styling or layout logic. Only update navItems array and icon imports.
  </action>
  <verify>
    <automated>grep -n "from '@/components/ui/badge'" apps/web/app/(protected)/admin/AdminSidebar.tsx && grep -n "BookOpen" apps/web/app/(protected)/admin/AdminSidebar.tsx && grep -n "'/admin/overview'" apps/web/app/(protected)/admin/AdminSidebar.tsx && grep -n "'/admin/bible'" apps/web/app/(protected)/admin/AdminSidebar.tsx</automated>
  </verify>
  <done>
    AdminSidebar.tsx updated with:
    - Import from @/components/ui/badge (not @query-stream-ai/ui)
    - BookOpen icon imported
    - All 7 navigation items (Overview, AI Usage, Challenges, Bible, Users, Feedback, Team Settings)
    - Overview and Bible are new navigation items for Phase 4 and Phase 5
  </done>
</task>

<task type="auto">
  <name>Task 2: Ensure admin layout enforces role-based access control</name>
  <files>apps/web/app/(protected)/admin/layout.tsx</files>
  <read_first>
    1. apps/web/app/(protected)/admin/layout.tsx — current admin layout
    2. apps/admin/app/admin/layout.tsx — admin app layout for comparison
  </read_first>
  <action>
Verify and ensure admin layout at apps/web/app/(protected)/admin/layout.tsx has proper auth checks:

1. Keep existing `export const dynamic = 'force-dynamic'` directive

2. Verify auth helper imports are correct: import { getServerUser, isLeaderOrAbove } from '@query-stream-ai/auth/server'

3. Verify auth check logic exists:
   - const user = await getServerUser()
   - if (!user) { redirect('/auth') }
   - if (!isLeaderOrAbove(user)) { redirect('/') }

4. Verify AdminSidebar is imported: import AdminSidebar from './AdminSidebar'

5. Verify layout structure:
   - Return flex container with sidebar and main content area
   - Pass userRole to AdminSidebar: <AdminSidebar userRole={user.role || 'user'} />

6. If anything is missing from the web app layout, copy from apps/admin/app/admin/layout.tsx (they should be identical)

DO NOT change the auth logic or layout structure. They are already correct in both files.
  </action>
  <verify>
    <automated>grep -n "getServerUser" apps/web/app/(protected)/admin/layout.tsx && grep -n "isLeaderOrAbove" apps/web/app/(protected)/admin/layout.tsx && grep -n "redirect('/auth')" apps/web/app/(protected)/admin/layout.tsx && grep -n "redirect('/')" apps/web/app/(protected)/admin/layout.tsx</automated>
  </verify>
  <done>
    Admin layout verified/updated with:
    - getServerUser() call for authentication
    - isLeaderOrAbove() check for role-based access
    - Redirect to /auth if not authenticated
    - Redirect to / if not leader or above
    - AdminSidebar imported and rendered with userRole prop
    - Dynamic export for force-dynamic rendering
  </done>
</task>

<task type="auto">
  <name>Task 3: Create overview page placeholder for Phase 4 dashboard</name>
  <files>apps/web/app/(protected)/admin/overview/page.tsx</files>
  <read_first>
    1. apps/web/app/(protected)/admin/page.tsx — existing admin home page for reference
    2. .planning/REQUIREMENTS.md — Phase 4 requirements (DASH-01 through DASH-17)
  </read_first>
  <action>
Create overview page placeholder at apps/web/app/(protected)/admin/overview/page.tsx:

1. Create a simple placeholder page that indicates this will be the dashboard in Phase 4:
   - Import Link from next/link
   - Import Button from @/components/ui/button
   - Import Card, CardContent, CardHeader, CardTitle from @/components/ui/card

2. Page content:
   - Title: "Admin Dashboard"
   - Description: "Overview dashboard will be implemented in Phase 4"
   - Quick links card with buttons to existing admin pages:
     - AI Usage (/admin/ai-usage)
     - Challenges (/admin/challenges)
     - Users (/admin/users)
     - Feedback (/admin/feedback)
     - Team Settings (/admin/team-settings)

3. Use card layout from shadcn/ui for consistent styling

4. Use exact imports: import { Button } from '@/components/ui/button', import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

DO NOT implement actual dashboard functionality. That's Phase 4 work.
  </action>
  <verify>
    <automated>grep -n "Admin Dashboard" apps/web/app/(protected)/admin/overview/page.tsx && grep -n "Phase 4" apps/web/app/(protected)/admin/overview/page.tsx && grep -n "from '@/components/ui/card'" apps/web/app/(protected)/admin/overview/page.tsx</automated>
  </verify>
  <done>
    Overview page exists at apps/web/app/(protected)/admin/overview/page.tsx with:
    - Title "Admin Dashboard"
    - Description mentioning Phase 4 implementation
    - Card layout with quick links to existing admin pages
    - All imports from @/components/ui/* (not @query-stream-ai/ui/*)
    - Placeholder content only (no actual dashboard logic)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 02 completes:
    - AdminSidebar updated with all 7 navigation items (Overview, AI Usage, Challenges, Bible, Users, Feedback, Team Settings)
    - Admin layout verified with role-based access control
    - Overview page placeholder created for Phase 4

    All admin pages now use the unified sidebar and layout.
  </what-built>
  <how-to-verify>
    1. Start dev server: cd apps/web && pnpm dev
    2. Visit http://localhost:3000/admin/overview
    3. Verify:
       - Page loads without errors
       - Admin sidebar shows all 7 navigation items (Overview, AI Usage, Challenges, Bible, Users, Feedback, Team Settings)
       - Overview page displays placeholder content
    4. Click each navigation item:
       - /admin/ai-usage — should load AI usage page
       - /admin/challenges — should load challenges page
       - /admin/bible — should show 404 (not implemented until Phase 5)
       - /admin/users — should load users page
       - /admin/feedback — should load feedback page
       - /admin/team-settings — should load team settings page
    5. Verify role badge shows correct role in sidebar header
    6. Verify sidebar highlights active route correctly
  </how-to-verify>
  <resume-signal>Type "approved" if sidebar navigation works correctly and all pages load, or describe issues</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. Verify AdminSidebar has all 7 navigation items: `grep -c "href:" apps/web/app/(protected)/admin/AdminSidebar.tsx` should return 7
2. Verify all imports use @/components/ui/* pattern: `grep "@query-stream-ai/ui" apps/web/app/(protected)/admin/AdminSidebar.tsx` should return nothing
3. Verify overview page exists: `test -f apps/web/app/(protected)/admin/overview/page.tsx`
4. Verify admin layout has auth checks: `grep -n "isLeaderOrAbove" apps/web/app/(protected)/admin/layout.tsx`
5. Verify TypeScript compiles: `cd apps/web && npx tsc --noEmit`
</verification>

<success_criteria>
1. AdminSidebar shows all 7 navigation items (Overview, AI Usage, Challenges, Bible, Users, Feedback, Team Settings)
2. All admin components use @/components/ui/* imports (not @query-stream-ai/ui/*)
3. Admin layout enforces role-based access control (leader and above)
4. Overview page placeholder exists for Phase 4
5. ADM-01 satisfied: "Unified admin interface for both Bible and Challenges management"
6. ADM-02 satisfied: "Consistent admin navigation and patterns across all admin panels"
7. ADM-06 satisfied: "Create shared admin layout/sidebar component"
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-admin-unification/01-02-SUMMARY.md` with:
- Navigation items added
- Import standardization completed
- Overview page created
- Ready for next plan (delete duplicate apps/admin)
</output>
